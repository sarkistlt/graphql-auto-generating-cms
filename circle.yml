machine:
  timezone:
    America/Los_Angeles
  node:
    version: 4

dependencies:
  pre:
    - npm i
    - npm install -g mocha istanbul@1.0.0-alpha.2

test:
  post:
    - npm run test:all && npm run coveralls