/*
import React from 'react';
import { mount, shallow } from 'enzyme';
import {expect} from 'chai';
import Layout from '../lib';
const wrapper = shallow(<Layout />);

describe('Layout page of CMS', () => {
  it('check all props', () => {
    expect(wrapper).to.have.props('newMenuItemSecret');
    expect(wrapper).to.have.prop('viewData');
    expect(wrapper).to.have.prop('listData');
    expect(wrapper).to.have.prop('currentPathSchema');
    expect(wrapper).to.have.prop('currentPathName');
    expect(wrapper).to.have.prop('SideMenuItems');
    expect(wrapper).to.have.prop('viewMode');
    expect(wrapper).to.have.prop('currentItemId');
    expect(wrapper).to.have.prop('offset');
    expect(wrapper).to.have.prop('lastPage');
    expect(wrapper).to.have.prop('fields');
  });
});
*/
